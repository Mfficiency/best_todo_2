/// Shared spacing scale. Using named steps instead of scattered magic numbers
/// keeps padding and gaps consistent across every screen — change the rhythm of
/// the whole app in one place.
class AppSpacing {
  AppSpacing._();

  static const double xs = 4;
  static const double sm = 8;
  static const double md = 12;
  static const double lg = 16;
  static const double xl = 24;
}
