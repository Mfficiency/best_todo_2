<?xml version="1.0" encoding="utf-8"?>
<!-- 1x1 icon-only widget: nothing but a tappable "+" that logs a Food Diary
     entry straight from the home screen. See food_diary_widget_layout.xml
     for the larger widget this is a companion to. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/food_button_widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <TextView
        android:id="@+id/food_button_widget_plus"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="+"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textSize="28sp" />
</FrameLayout>
